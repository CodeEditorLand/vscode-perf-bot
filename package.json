{
	"bin": {
		"vscode-perf-bot": "bin/vscode-perf-bot"
	},
	"dependencies": {
		"@octokit/rest": "21.0.2",
		"@slack/web-api": "7.7.0",
		"chalk": "5.x",
		"commander": "12.1.0"
	},
	"description": "Allows to run performance tests with Slack bot messages for VS Code.",
	"devDependencies": {
		"@types/node": "22.10.1"
	},
	"main": "out/index",
	"name": "@codeeditorland/vscode-perf-bot",
	"scripts": {
		"build": "tsc -p ./",
		"compile": "tsc -p ./",
		"prepare": "npm run build"
	}
}
